import 'package:flutter/material.dart';

// Placeholder untuk Blog Form
class BlogFormPage extends StatelessWidget {
  const BlogFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Blog Form'),
        backgroundColor: const Color(0xFF6B8E72),
        foregroundColor: Colors.white,
      ),
      body: const Center(child: Text('Blog Form Page - Coming Soon')),
    );
  }
}
